# Bestell Bot Voice - Konfiguration
# Kopiere diese Datei nach .env und fülle die Werte aus

# === SIP Konfiguration (Sipgate) ===
SIP_SERVER=sipconnect.sipgate.de
SIP_USERNAME=your_sip_id
SIP_PASSWORD=your_sip_password
SIP_PORT=5060

# === OpenAI API ===
OPENAI_API_KEY=sk-your-api-key-here
# Verfügbare Models:
#   - gpt-realtime (Standard, neues verbessertes Model seit Aug 2025)
#   - gpt-4o-realtime-preview-2024-12-17 (älteres Preview-Model)
OPENAI_MODEL=gpt-realtime

# === Audio Einstellungen ===
AUDIO_SAMPLE_RATE=16000
AUDIO_CHANNELS=1
AUDIO_FRAME_DURATION_MS=20

# === Auto-Accept ===
# Wenn true, werden eingehende Anrufe automatisch angenommen und mit AI verbunden
AUTO_ACCEPT_CALLS=true

# === Logging ===
LOG_LEVEL=INFO
